# AI Project & Deployment Reference

This file serves as a reference for AI assistants (Gemini, Claude, etc.) regarding the deployment infrastructure and workspace guidelines for Mellow Belly.

## Git Guidelines (CRITICAL)
* **No Auto-Commits:** NEVER create git commits without asking the user for explicit permission first.
* **No Auto-Pushes:** NEVER push commits to GitHub without asking the user for explicit permission first.

## Infrastructure & Deployment
* **Frontend Hosting:** Vercel (automatically deployed on pushes to the `main` branch of the GitHub repository `FerchizV/mellow-belly`).
* **Database Hosting:** Supabase Cloud (online cloud-hosted instance, Project ID: `pspohoscnhigvhwsmmiw`).
* **Development Flow:** All development is done directly in this local environment. There is no active local development database or local Supabase CLI setup.

## Database Guidelines
* **Database Schema:** The source of truth for the database schema is the live Supabase Cloud database. A clean reference backup of this schema is maintained in [supabase/schema.sql](file:///c:/Users/ferch/Documents/Coding/Mellow-Belly/mellow-belly/supabase/schema.sql).
* **Migrations:** Local migration files (`supabase/migrations/*.sql`) are not used since database changes are managed directly via the online Supabase Dashboard. Do not generate or depend on local migration files unless explicitly requested.
